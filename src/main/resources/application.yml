spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true
      path: /h2-console
  security:
    # Setzen Sie diesen Wert auf false, um Spring Security zu deaktivieren
    enabled: true

